#include "reversestring.h"
#include<vector>
#include<cstring>


/*reversestring::reversestring(string p)
{
	int p1 = (p.length() + 1);
	char *x1 = new char[p1];
	strcpy(x1, p.c_str());

	char * p3 = std::strtok(x1, " ");
	//c_str converts string to charecter array// 
	for (int i =p1; i !='/0'; i++)
	{
		x.push_back(p3[i]);
  	}
	for (int i = p.size(); i != '/0'; i++)
	{
		cout << x[i] << endl;
	}
	delete(x1);
}*/


reversestring::~reversestring()
{
	
}
